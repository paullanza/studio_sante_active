<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to "Studio Santé Active",
                user_signed_in? ? dashboard_path_for(current_user) : unauthenticated_root_path,
                class: "navbar-brand" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
            aria-expanded="false" aria-label="Basculer la navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <% if user_signed_in? %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to "Nouvelle séance", new_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Tableau de bord", dashboard_path_for(current_user), class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Client·e·s", fliip_users_path, class: "nav-link" %>
          </li>
          <% if current_user.admin? || current_user.super_admin? %>
            <li class="nav-item">
              <%= link_to "Confirmations", admin_unconfirmed_sessions_path, class: "nav-link" %>
            </li>
          <% end %>
        </ul>
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <%= link_to current_user.full_name, user_path(current_user), class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Déconnexion",
                        destroy_user_session_path,
                        data: { turbo_method: :delete },
                        class: "nav-link" %>
          </li>
        </ul>
      <% else %>
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <%= link_to "Connexion", new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Créer un compte", new_user_registration_path, class: "nav-link" %>
          </li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
