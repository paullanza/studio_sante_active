<div class="auth-page min-vh-100 d-flex align-items-center justify-content-center p-4">
  <div class="auth-card w-100" style="max-width: 640px;">
    <h1 class="mb-2 fw-bold">Modifier le profil</h1>

    <div class="mb-4 small text-muted">
      <%= link_to "Retour au profil", user_path(@user), class: "link-brand fw-semibold" %>
    </div>

    <%= simple_form_for(@user, html: { class: "auth-form" }) do |f| %>
      <%= f.error_notification %>

      <div class="row g-3">
        <div class="col-md-6">
          <%= f.input :first_name,
                label: "Prénom",
                input_html: { class: "form-control form-control-lg" } %>
        </div>
        <div class="col-md-6">
          <%= f.input :last_name,
                label: "Nom",
                input_html: { class: "form-control form-control-lg" } %>
        </div>
      </div>

      <div class="row g-3 mt-0">
        <div class="col-md-6">
          <%= f.input :phone,
                label: "Téléphone",
                as: :tel,
                input_html: { autocomplete: "tel", class: "form-control form-control-lg" } %>
        </div>
        <div class="col-md-6">
          <%= f.input :birthday,
                label: "Date de naissance",
                as: :string,
                input_html: { type: "date", max: Date.today, class: "form-control form-control-lg" } %>
        </div>
      </div>

      <!-- Adresse (Mapbox autocomplete; underlying input stays as the model source) -->
      <div class="mb-3">
        <%= f.input :address,
              label: "Adresse",
              input_html: {
                class: "form-control form-control-lg d-none",
                data: { address_autocomplete_target: "address" }
              },
              wrapper_html: {
                class: "position-relative",
                data: {
                  controller: "address-autocomplete",
                  address_autocomplete_api_key_value: ENV["MAPBOX_API_KEY"]
                }
              } %>
      </div>

      <div class="mb-3">
        <%= f.input :email,
              label: "Courriel",
              input_html: { autocomplete: "email", class: "form-control form-control-lg" } %>
      </div>

      <div class="d-grid mt-4">
        <%= f.button :submit, "Enregistrer",
              class: "btn btn-lg rounded-pill shadow-sm btn-brand" %>
      </div>
    <% end %>

    <hr class="my-4">
    <div class="d-flex flex-wrap gap-2">
      <%= link_to "Annuler", user_path(@user), class: "btn btn-outline-secondary btn-sm" %>
    </div>
  </div>
</div>
